package main.java;

import GUI.LoginForm;

/**
 * The type Main.
 */
public class Main
{
    /**
     * The entry point of application. We start with the login form
     *
     * @param args the input arguments
     */
    public static void main(String[] args)
    {
        LoginForm start = new LoginForm(null);
        //RegistrationForm test = new RegistrationForm(null);
        //AdminForm adminForm = new AdminForm(null);
    }
}
